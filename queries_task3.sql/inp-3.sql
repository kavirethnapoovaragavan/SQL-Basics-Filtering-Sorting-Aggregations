/* ---------------------------------------------------------
   4. FILTERING USING WHERE
   --------------------------------------------------------- */

-- Fetch all Technology category orders
SELECT *
FROM retail_sales
WHERE category = 'Technology';
