# SQL-Basics-Filtering-Sorting-Aggregations
This project demonstrates basic SQL skills using the Superstore dataset. It covers data import, filtering, sorting, and aggregation using WHERE, ORDER BY, GROUP BY, HAVING, BETWEEN, and LIKE clauses. Query results are exported to CSV and documented with business insights.
